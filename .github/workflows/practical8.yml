name: Practical 8 - Scheduled Workflow

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
  
jobs:
  scheduled-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Run Scheduled Script
        run: python Prac8/app.py